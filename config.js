/* Public site config. Edit these values. They are visible in the page source. */
window.PLEDGE_CONFIG = {
  /* Where signature submissions are emailed. Uses formsubmit.co. No GitHub account required. */
  signatureEmail: "sara@niftyz.io",
  /* Optional: paste a Google Form or Tally URL to embed instead of the on-page form. */
  signFormUrl: "",
  /* Optional custom domain later, e.g. https://recital133.org */
  canonicalUrl: "",
};
